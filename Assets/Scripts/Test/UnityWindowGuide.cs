using UnityEngine;

public class UnityWindowGuide : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hierarchy : 현재 씬 안의 오브젝트 목록 출력");
    }
}
